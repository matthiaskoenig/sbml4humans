# the build which ships with the python package (npm run build:package): the local server of
# sbml4humans.show serves the page and the api from one origin, and nothing is reported
VITE_API_URL=/api
VITE_DOCS_URL=https://matthiaskoenig.github.io/sbml4humans/
VITE_ANALYTICS=off
